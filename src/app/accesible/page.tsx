import { AccesibleEntryPage } from "@/components/alae/accesible-entry-page";

export const metadata = {
  title: "Acceso con lectura por voz — FORGE",
  description:
    "Activa el modo lectura por voz de FORGE sin instalar aplicaciones adicionales.",
};

export default function AccesiblePage() {
  return <AccesibleEntryPage />;
}
